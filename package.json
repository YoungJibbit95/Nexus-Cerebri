{
  "name": "nexus-cerebri-tooling",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/check-repository.mjs",
    "docs:build": "npm --prefix apps/cerebri-site run build",
    "docs:check": "npm --prefix apps/cerebri-site run check",
    "test:node": "node --test bindings/node/test.mjs"
  },
  "devDependencies": {
    "markdown-it": "14.3.2"
  }
}
