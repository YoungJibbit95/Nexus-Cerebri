# Datasets

Foundation uses only [synthetic CPIR fixtures](../examples/request.json).
Future datasets require version/checksum manifests and explicit Synthetic, GlobalCurated,
PersonalLocal or OptInShared scope. PersonalLocal means logically scoped to one person,
not a promise about physical storage location. Personal feedback is not shared by default.
No training corpus is included.

