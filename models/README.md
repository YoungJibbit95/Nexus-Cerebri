# Models

No trained model or model runtime is included. Production metadata and the immutable
ModelRegistry port live in cerebri-ml. Artifacts are versioned and checksum-addressed;
model versions are separate from software, CPIR and dataset versions.
Generated artifacts/ are ignored.

